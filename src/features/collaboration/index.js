// Collaboration domain - handles sharing, shared content, statistics
import routes from './routes.js';
import store from './store.js';

export default {
  name: 'collaboration',
  routes,
  store,
};