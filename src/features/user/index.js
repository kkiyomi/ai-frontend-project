// User domain - handles user account, profile, authentication (placeholder for scaling)
import routes from './routes.js';
import store from './store.js';

export default {
  name: 'user',
  routes,
  store,
};