// Central export point for all mock data
export { default as mockSeries } from './series';
export { default as mockChapters } from './chapters';
export { default as mockGlossaryTerms } from './glossaryTerms';

// Re-export types for convenience
export type { Series, Chapter, GlossaryTerm } from '../types';